{
  "name": "graphql-continuations",
  "version": "0.1.1",
  "description": "An alternative approach to @defer in resolving expensive GraphQL fields",
  "main": "./cjs/index.js",
  "scripts": {
    "clean": "rm -rf cjs && rm -rf mjs",
    "build": "pnpm clean && pnpm build:mjs && pnpm build:cjs",
    "build:mjs": "tsc -p tsconfig.mjs.json",
    "build:cjs": "tsc -p tsconfig.json",
    "test": "vitest",
    "prepublish": "pnpm build",
    "coverage": "vitest run --coverage",
    "watch": "nodemon --exec \"spec-md > ./docs/index.html\" ./spec/spec.md"
  },
  "exports": {
    ".": {
      "import": {
        "types": "./mjs/index.d.ts",
        "default": "./mjs/index.js"
      },
      "require": {
        "types": "./cjs/index.d.ts",
        "default": "./cjs/index.js"
      }
    }
  },
  "types": "./cjs/index.d.ts",
  "files": [
    "mjs/*",
    "cjs/*"
  ],
  "keywords": [
    "graphql",
    "defer"
  ],
  "author": "Tim Griesser",
  "license": "MIT",
  "dependencies": {
    "uuid": "^9.0.0"
  },
  "peerDependencies": {
    "graphql": "*"
  },
  "devDependencies": {
    "@types/uuid": "^9.0.0",
    "graphql": "^16.6.0",
    "graphql-tag": "^2.12.6",
    "nodemon": "^2.0.20",
    "spec-md": "^3.0.2",
    "ioredis": "^5.2.4",
    "typescript": "^4.9.4",
    "vitest": "^0.25.7"
  }
}
