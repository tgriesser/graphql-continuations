export const CONTINUATION_TYPE_NAME = "Continuation" as const;

export const RESOLVER_SYMBOL = Symbol.for("graphql-continuations/resolver");

export const CONTINUATION_RACE = Symbol.for("graphql-continuations");
